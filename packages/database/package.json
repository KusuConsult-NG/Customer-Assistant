{
  "name": "@ace/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "generate": "prisma generate",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed:gatekipa": "node prisma/seed-gatekipa.js",
    "clean": "rm -rf dist tsconfig.tsbuildinfo"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0"
  },
  "devDependencies": {
    "prisma": "^6.3.0",
    "typescript": "^5.7.3"
  }
}
