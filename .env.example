# ═══════════════════════════════════════════════════════════════════════════
# ACE Platform — local development environment.
#
#   cp .env.example .env
#
# Then edit DATABASE_URL if your Postgres is elsewhere. Everything else works
# as written for a local run.
#
# ⚠ POINT THIS AT A LOCAL DATABASE, NEVER PRODUCTION. The validation harness
#   and channel probes create real organizations through the real API — that
#   is why they catch bugs mocks do not. Pointed at production once, they left
#   358 test organizations and ~13,900 contacts sitting in the live CRM.
# ═══════════════════════════════════════════════════════════════════════════


# ─── Database (required) ───────────────────────────────────────────────────
# Locally both point at the same database. In production DATABASE_URL uses the
# Supabase pooler in transaction mode (port 6543, pgbouncer=true) and
# DIRECT_URL stays on 5432 for migrations.
DATABASE_URL=postgresql://ace@127.0.0.1:5432/ace_dev
DIRECT_URL=postgresql://ace@127.0.0.1:5432/ace_dev


# ─── Auth (required — the API refuses to boot without these) ───────────────
# No fallback exists anywhere in the code, deliberately. Minimum 32 characters.
# Generate real ones with:  openssl rand -base64 48
JWT_SECRET=local-dev-only-jwt-secret-change-me-0123456789abcd
JWT_REFRESH_SECRET=local-dev-only-refresh-secret-change-me-0123456789
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d


# ─── AI provider (required) ────────────────────────────────────────────────
# ⚠ Startup validation REJECTS any value containing the word "placeholder".
#   That check is deliberate — it caught real deploys shipping with dummy
#   keys — so use a real-looking dummy locally, not the word placeholder.
#
# The key authenticates whichever OpenAI-COMPATIBLE provider LLM_BASE_URL
# points at. Several serve the same wire format on a free plan (Groq, Gemini's
# OpenAI-compat endpoint, OpenRouter, Cerebras) — check their current terms.
# With no working key the assistant still answers from curated FAQs and hands
# off everything else honestly; it never invents an answer.
OPENAI_API_KEY=sk-local-dev-0000000000000000
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_CHAT_MODEL=gpt-4o-mini
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_DIMENSIONS=1536


# ─── WhatsApp webhook validation (required to boot) ────────────────────────
# WHATSAPP_APP_SECRET: Found in Meta Developer Portal → Your App → Settings → Basic
#   → App Secret. Used to verify X-Hub-Signature-256 on every inbound webhook POST.
# WHATSAPP_VERIFY_TOKEN: A token YOU choose; paste the SAME value into Meta
#   Developer Console → WhatsApp → Configuration → Webhook → Verify Token.
# NOTE: Per-organization access tokens + phone number IDs are stored in the DB
#   (Settings → WhatsApp Integration), NOT here.
WHATSAPP_APP_SECRET=replace_with_meta_app_secret_from_developer_portal
WHATSAPP_VERIFY_TOKEN=replace_with_your_chosen_verify_token


# ─── Optional: everything below degrades honestly when unset ───────────────

# Redis. Left UNSET on purpose: without it the app degrades cleanly to single
# pod — uploads index inline (text types only), workflows run on the inline
# sweeper, rate limiting is in-memory. Set it only if Redis is actually running.
# Pointing it at a Redis that is not there is worse than leaving it unset: the
# BullMQ workers retry in a loop and flood the log with ECONNREFUSED.
# REDIS_URL=redis://127.0.0.1:6379

# Public URLs. WEB_BASE_URL is used to build emailed verification links and
# selfie-capture links; unset, customers receive localhost links.
API_BASE_URL=http://localhost:4000
WEB_BASE_URL=http://localhost:3000
# Comma-separated. Both spellings of the loopback address are listed because
# the browser treats them as different origins: open the dashboard on
# 127.0.0.1 with only localhost allowed and every API call fails CORS, which
# shows up as a login that does nothing. (/api/widget/* is open to all origins
# regardless — embeds must work on customer domains.)
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# File uploads. Both buckets must be created manually and be PRIVATE:
# knowledge-documents and onboarding-selfies. Unset, document upload and
# selfie capture return an honest 503 and nothing else is affected.
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=

# Voice. All three are needed before a call can be answered. Unset, the voice
# probe BLOCKS rather than reporting failure — an unconfigured channel and a
# broken one must never look alike.
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=
# DEEPGRAM_API_KEY=
# ELEVENLABS_API_KEY=

# Hosted conversational agent (ElevenLabs Agents).
#
# Only OUTBOUND needs a key — we initiate those. INBOUND needs nothing here:
# the agent authenticates to us with its own per-organization agent key, minted
# by scripts/mint-agent-key.js and sent as a bearer token to /api/agent-tools.
#
# The agent id and the WhatsApp number id are per TENANT, not per environment,
# so they live in HostedAgentConfig rather than here: a wrong agent id means one
# tenant's customers hear another tenant's agent.
#
# SO DOES THE WORKSPACE KEY, and this is a security boundary rather than a
# convenience. An ElevenLabs workspace has no tenancy of its own: the agents,
# the phone numbers, the WhatsApp lines and every conversation transcript belong
# to whoever holds the key. Put every tenant behind one shared key and the only
# thing keeping their customers apart is our own filtering being right at every
# listing endpoint anyone ever adds.
#
# So a tenant with no key of its own is REFUSED, not quietly shared. Give each
# one its own key through POST /api/agent-provisioning/credentials. The single
# exception is a deployment that genuinely serves one tenant — a demo, local
# development, a single-business install — where the distinction is moot:
# ELEVENLABS_ALLOW_SHARED_WORKSPACE=1
#
# Deliberately not inferred from NODE_ENV. A boundary that changes shape
# depending on a variable nobody set on purpose is one nobody can reason about.
#
# Set the base URL only for a data-residency deployment — the default is the US
# endpoint, and a tenant bound to a jurisdiction should not be routed through it.
# ELEVENLABS_BASE_URL=https://api.eu.residency.elevenlabs.io
#
# Encryption for every third-party credential stored in the database: the
# per-tenant ElevenLabs workspace key, Twilio auth tokens and API secrets, and
# WhatsApp access tokens. Between them they can read every call transcript,
# place calls the tenant is billed for, and send messages as the business — so a
# database backup or a leaked read replica should not hand them over.
#
#   openssl rand -base64 32
#
# Unset, storing a tenant credential is REFUSED rather than written in the
# clear — including saving Twilio or WhatsApp settings from the dashboard.
# Values written before this existed still read back, and warn loudly on every
# read until `npm run secrets:encrypt -- --apply` has rewritten them.
#
# KEEP THIS KEY. Lost, the stored credentials cannot be read back and every
# affected tenant needs a fresh key from ElevenLabs.
# ENCRYPTION_KEY=
#
# Only during a rotation: set ENCRYPTION_KEY to the new key, move the old one
# here, run `npm run secrets:encrypt -- --apply`, then delete this line.
# ENCRYPTION_KEY_PREVIOUS=

# Signing secret for the SHARED workspace's post-call webhook, from its webhook
# settings. Unset, /api/webhooks/elevenlabs answers 500 — so ElevenLabs retries
# and the transcripts are delayed rather than trusted unverified, or lost.
#
# This verifies ONE workspace, because a secret can only verify the workspace
# that holds it. A tenant with its own workspace stores its own secret (POST
# /api/agent-provisioning/credentials) and points that workspace's post-call
# webhook at /api/webhooks/elevenlabs/<organizationId> — the organization is in
# the path because the signature has to be checked before the body is parsed,
# which rules out reading the tenant out of the payload.
# ELEVENLABS_WEBHOOK_SECRET=
#
# Only if the signature header is not called ElevenLabs-Signature. That name is
# what their docs use but has never been confirmed here against a live delivery;
# a rejected webhook logs the header names it actually carried, so the first real
# one will tell you whether this needs setting.
# ELEVENLABS_SIGNATURE_HEADER=ElevenLabs-Signature

# Vector search. Unset, knowledge search uses the PostgreSQL keyword fallback.
# QDRANT_URL=
# QDRANT_API_KEY=

# Email delivery. Unset, registration returns the verification link in the
# response instead of sending it, and says so.
# RESEND_API_KEY=

# Payments.
# PAYSTACK_SECRET_KEY=
# PAYSTACK_WEBHOOK_SECRET=

# Behind a load balancer only. Without it every user is rate-limited as one
# shared IP; with it set on a directly-exposed deploy, a forged
# X-Forwarded-For can spoof the client IP. Opt in deliberately.
# TRUST_PROXY=1

# ─── Public premium payment portal (PLASCHEMA) ───────────────────────────────
# The organization slug that /api/public/pay/* is scoped to. These routes are
# unauthenticated by necessity — a citizen paying a premium has no account — so
# the tenant comes from here rather than from the request. Unset, the routes
# refuse every call; they never fall back to searching across all tenants.
PUBLIC_PAYMENT_ORG_SLUG=
